API: finnhub.io/docs/api

MVCs and Associations:

User 
*build out later - can use dakota's eventfull walkthroughs*
has_many :portfolios
:name - string
:email - string
:bio - string
:image_url - string
:password -string 
:password_digest -string
(include any other verification attributes, validations, and has_secure_password)

!Portfolio X
has_many :stocks
has_many :cryptoexchanges  
has_many :portfolio_stocks, :through :stocks
*belongs_to :user --build out later--*
:s - string (symbol from API)  
:p (last price) -float
// cryptoexchange 
:cryptoexchanges, -array * use get /crypto/exchange to get an array of supported cryptoexchanges *

*reminder: key here is to access data by traversing the nested objects of the stock object stocks[companyprofile2])*
!Stock X
belongs_to :portfolio 
belongs_to :portfolio_stock
:s (symbol), - string,
:p (last price), -float,
// below is stock data from Company Profile 2 https://finnhub.io/docs/api/company-profile2
:country, -string,
:currency, -string,
:exchange, -string,
:ipo, -string,
:marketCapitalization, -bigInt,
:finnhubIndustry -string
//below is data from Peers finnhub.io. attribute is :symbol 
:peers, -array,
//below is data from Recommendation Trends from finnhub.io 
:buy, -integer,
:hold, -integer,
:sell, -integer,
:strongBuy, -integer,
:strongSell, -integer,
//* explore where P/E ratio is nested in Basic Financials 
:portfolio_id, -bigInt
:updated_at, -datetime --> this data is included in the table

!CryptoExchange X 
belongs_to :portfolio
belongs_to :portfolio_stock
:symbol, -string,
:displaySymbol, -string,
:description, -string,
:portfolio_id, -bigInt

!Portfolio_Stock X
*can use this later to show amount of stock in portfolio*
has_many :stocks
has_many :cryptoexchanges
has_many :portfolios, :through :stocks
:portfolio_id, -bigInt
:stock_id, -bigInt 
:cryptoexchange_id, -bigInt

?Category 
*review relationship and build out later (review Ayana's videos)
has_many :stocks, dependent: :destroy 
:finnhubIndustry, string (note: In companyProfile2 in finnhub API)
 




Future Additional Features:
-candlestick data widget?
-feature that returns all companies in a category with a PE ratio of range .15-.19
-news feature using Market News and Company News 
//Market News
:category, -string,
:headline, -string,
:summary, -string,
:source, -string 
//Company News
:category, -string,
:related, -string,
:headline, -string,
:source, -string,
:summary, -string,
:url, -string
-recommendation trends widget 
-candlestick data widget 
-display crypto candles widget 
-run pattern recognition data on a symbol 
-Covid19 data based on US state - from CDC API 

